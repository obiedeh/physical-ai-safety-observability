# Roadmap

1. Add RTSP frame extraction behind the existing source loader.
2. Wire `OpenAICompatibleAdapter` through config and secrets.
3. Add camera calibration and richer zone geometry.
4. Persist events and incidents in Postgres or SQLite.
5. Add operator dashboard and human review workflow.
6. Add Jetson telemetry probes for thermals and GPU pressure.
7. Add incident export and immutable audit trails.

