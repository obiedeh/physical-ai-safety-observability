# Evidence Chain

Evidence captures the frame hash, source URI, adapter name, model version, rule version,
capture timestamp, telemetry snapshot, and detections used by the rule. This keeps events
auditable and replayable without binding the system to one model vendor.

